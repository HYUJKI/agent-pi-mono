{
  "name": "pimono-agent",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "app",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/ai && npm run build --workspace=packages/agent && npm run build --workspace=app",
    "dev": "npm run dev --workspace=app",
    "dev:server": "npm run dev --workspace=server",
    "dev:proxy": "node proxy.mjs",
    "dev:all": "npm run dev:proxy & npm run dev:server & npm run dev",
    "clean": "rm -rf packages/*/dist app/dist server/dist node_modules"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}